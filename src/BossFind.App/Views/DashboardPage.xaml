<Page
    x:Class="BossFind.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Loaded="OnLoaded">
    <ScrollViewer>
        <StackPanel MaxWidth="980" Margin="32" Spacing="20">
            <StackPanel Spacing="6">
                <TextBlock Text="工作台" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock Text="候选人数据与本地运行状态" Style="{StaticResource BodyTextBlockStyle}" />
            </StackPanel>
            <ProgressRing Width="28" Height="28" IsActive="{Binding IsBusy}" />
            <ItemsRepeater ItemsSource="{Binding ReadinessItems}">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,14" ColumnDefinitions="180,*">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="1" Text="{Binding Status}" TextWrapping="Wrap" />
                        </Grid>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
            <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Opacity="0.72" />
        </StackPanel>
    </ScrollViewer>
</Page>
